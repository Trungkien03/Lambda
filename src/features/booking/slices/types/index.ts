type bookingViewState = {
  isLoadingAddBooking: boolean;
};

const initialBookingViewState: bookingViewState = {
  isLoadingAddBooking: false,
};

export { initialBookingViewState };
export type { bookingViewState };
